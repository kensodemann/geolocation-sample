import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.sacandidatesample',
  appName: 'sa-candidate-sample',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
